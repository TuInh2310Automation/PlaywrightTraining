const MathConstant = require("../constants/MathConstant");
const NumberHandler = require("./NumberHandler");

console.log(`Is ${MathConstant.PI} an even number ${NumberHandler.isEvenNumber(MathConstant.PI)}`);
